# Placeholder npm policy — replace with your org's exact settings before broad reuse.
# min-release-age=7 — relaxed until 2026-08-16 for nanoid@3.3.17 security override
min-release-age=0
# Examples (uncomment and edit):
# engine-strict=true
# save-exact=true
# fund=false
# audit=true
#
# Private registry (do not commit secrets):
# @your-scope:registry=https://npm.example.com/
# //npm.example.com/:_authToken=${NPM_TOKEN}
